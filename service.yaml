apiVersion: v1
kind: Service
metadata:
  name: salary-prediction-service
spec:
  selector:
    app: salary-predictor
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8501
  type: LoadBalancer
