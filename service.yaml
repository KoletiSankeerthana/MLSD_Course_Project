apiVersion: v1
kind: Service

metadata:
  name: employee-salary-service

spec:
  type: NodePort

  selector:
    app: employee-salary-app

  ports:
    - protocol: TCP
      port: 8501
      targetPort: 8501
      nodePort: 30007